# Snívať — UI Redesign + Documentation Split

Two independent tracks. Approve once, I execute both.

---

## TRACK 1 — Documentation (AGENTS.md + PRODUCT.md split)

**Reality check:** the clean "# Snívať / Mission" draft you pasted in chat was never saved to disk. On-disk AGENTS.md is still the old TalentNex version (7442 bytes, Jul 7). This track lands your clean draft AND applies all 8 of your feedback points, then splits into two files.

### `PRODUCT.md` — Vision, brand, product (what & why)

1. **Mission** — from your clean draft (not a job board / not LinkedIn / the journey IS the content)
2. **Product Philosophy** — restored to the richer original version (you said "keep almost all of it"): growth is shared, discovery over application, profiles evolve, curiosity-driven not fear-driven, what makes people return
3. **Brand** *(new, your point #8)*
   - Name: **Snívať**
   - Meaning: Dream. Grow. Connect.
   - Voice: optimistic, friendly, honest, community-first
   - Keywords: Dream, Grow, Build, Connect
   - Avoid: corporate, LinkedIn language, buzzwords, fake startup copy
4. **Target Audience** — young builders in Nepal
5. **Non-negotiables** — never fake users/likes/comments/jobs/stats/notifications; if it's empty, show it's empty
6. **Product Priorities** — UX > reliability > community > performance > appearance
7. **Community First** — platform valuable even when nobody's hiring
8. **Growth Context** *(trimmed, your point #2 — no founder diary)* — keep the funnel (YouTube/TikTok → Discord → Snívať), the 4 phases, "no V2 rewrite" rule. Drop: 17 hours, sub counts, "based in Nepal," college-exam priorities. These don't change how the AI works.

### `AGENTS.md` — How to work (engineering & rules)

1. **Tech Stack** — Next.js 15, React 18.3.1 (NOT React 19), TypeScript, Prisma+SQLite, NextAuth v5, Tailwind, Server Actions
2. **Engineering Principles** — small changes, explain WHY before fixing, no blind agreement, challenge weak ideas, reversible, production-quality
3. **Core Principles** *(new, your point #6)* — community first, no engagement bait, no infinite-scroll tricks, no dark patterns, fast first paint, mobile-first, accessibility matters. Every feature must answer: *"Why will users return?"*
4. **Architecture Decisions** *(table — your point #5, replaces dated logs)*

   | Decision | Reason | Tradeoff |
   |---|---|---|
   | React 18 only — no `useActionState` | React 19 not in stack; use `useState`+`onSubmit` | More boilerplate than action API |
   | Yahoo OAuth registered as `as never` cast | No Auth.js v5 provider; custom OAuth object | Type hack, works at runtime |
   | V1 is permanent — no V2/rewrite | Restarting destroys momentum | Carries tech debt forward |
   | JWT session strategy + Prisma adapter | Edge middleware can't use Prisma Client | Tokens not revocable server-side |
   | `Post` reused across 4 categories | One model, one feed primitive for V1 | Job posts carry empty community fields |

5. **Testing Philosophy** — keep verbatim (you loved it): think like a first-time user, test full journeys through the UI
6. **Design Principles** — keep, **fix gradient rule**: "gradients reserved for brand mark only; UI chrome stays flat"
7. **Working Rules** — question assumptions, challenge decisions, honest feedback only
8. **Current State** — builds cleanly, demo login **corrected** to `demo@snivat.local / demo1234`

**Cleanup:** old `Known Issues` dated log → consumed into Architecture Decisions table. Founder diary → dropped. Every "TalentNex" → "Snívať" (your point #1).

---

## TRACK 2 — UI Redesign (Dream / Grow / Connect)

Your 3 decisions: theme=follow system · DNA canvas=keep+retheme · accent=my call → lift toward logo green `#2f9e6b`.

**Phase 1 — Tokens & accent**
- `globals.css`: light `--accent` `#2d6a4f`→`#2f9e6b`; fix header comment TalentNex→Snívať
- `ThemeProvider.tsx`: default accent `#dc2626`→`#2f9e6b`; first-visit theme reads `prefers-color-scheme`
- `layout.tsx`: no-FOUC inline script in `<head>`; metadata title → "Snívať — Dream. Grow. Connect."
- `settings/page.tsx` + `actions.ts`: default accent → `#2f9e6b`; add `#2f9e6b` as first swatch in `SettingsForm.tsx`

**Phase 2 — Logo & wordmark** (`Logo.tsx` rewrite)
- Mark: inline SVG with teal→lime gradient (brand signature). *Honest note: recreation, not pixel-match — swap in real asset if you have it.*
- Wordmark: `Sní` in `text-ink` (black) + `vať` in `text-accent` (user's color)

**Phase 3 — DNA canvas rethemed** (`DNABackground.tsx`)
- Delete hardcoded `PALETTES` RGB → `readTokens()` reads `--accent/--accent-soft/--moss/--deep/--warm` from computed styles; re-read on resize so theme/accent flips propagate
- `STRAND_COUNT` 7→5, lower opacities ~30% for restraint

**Phase 4 — Landing + home route** (new `page.tsx` + `Landing.tsx` rewrite)
- Create `src/app/page.tsx`: signed-out→`<Landing/>`, signed-in→redirect `/community` (route is currently missing!)
- Landing copy: "Where talent evolves"→"Dream. Grow. Connect."; drop "Partial DNA" section
- **Kill fake likes** (`Math.random()*200+20`) → real `reactionCounts()`

**Phase 5 — Brand & token cleanup**
- `Footer.tsx`: `tracking-[0.16em]`→`.eyebrow`; "Partial DNA"→"Snívať"; copyright copy
- `profile/[id]/page.tsx`: `text-zinc-*`+`dark:` → `text-ink*` tokens (worst offender)
- `PostComposer.tsx`/`ImageUploader.tsx`: `hover:border-zinc-400`→`hover:border-line-strong`
- `ImageUploader.tsx`: `bg-black/60 text-white`→`bg-ink/70 text-surface`
- `PostDetail.tsx`: drop no-op `prose prose-zinc` (typography plugin not installed)
- `signin/page.tsx`: `style={{boxShadow}}`→`shadow-lg`; OAuth buttons→`btn-outline`

**Phase 6 — Consistency** — standardize page rhythm (`px-5`), list spacing (`space-y-5`), shared button utilities

**Phase 7 — Build & functional verify** — `npm run build` clean; full UI journey walk (landing → sign-in → post → comment → react → profile → settings → jobs → applications); confirm no FOUC, accent propagates to wordmark+canvas+CTAs, fake likes gone

**Phase 8 — Mobile & Performance** *(your addition)*
- Responsive test at 320px, 375px, 390px, 768px, 1024px, 1440px
- Lighthouse audit (perf, a11y, best-practices, SEO)
- Accessibility audit (focus states, color contrast on new accent, reduced-motion respected by DNA canvas)
- Image optimization (uploads use raw `<img>` — audit for `next/image` candidates or at minimum proper width/height/sizes)
- Verify no layout shifts (CLS) — esp. on theme toggle and accent swap

---

**Scope:** ~18 files, all small/reversible except Logo rewrite + new page.tsx. No schema changes, no new deps, no logic changes (presentation + 1 bug fix + 1 missing route). Docs: 2 new files replacing 1.

**Execution order:** Track 1 (docs) first — small, fast, sets the contract. Then Track 2 phases 1→8 in order, committing logical groups so you can review incrementally.
